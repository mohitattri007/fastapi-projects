# Todo API (FastAPI)

A beginner-friendly FastAPI project with in-memory todo storage.

## Endpoints

- POST /todos
- GET /todos
- PUT /todos/{todo_id}
- DELETE /todos/{todo_id}

Run:

uvicorn main:app --reload